config pull origin master //sync local repo with git hub repo
config add 'file'
config commit -m 'string'
config push


//new comp stuff
//copys repo to new computer
git clone --bare https://github.com/Ph4nt4sm/neovim.git $HOME/.cfg
//not sure if needed
config checkout

